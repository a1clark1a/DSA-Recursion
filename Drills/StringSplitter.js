/*Write a recursive function that splits a string based on a separator (similar to String.prototype.split).
 Don't use JS array's split function to solve this problem. */

const stringSplitter = (input, separator) => {
  let arr = [];
  if (input === "") {
    return "";
  } else {
  }
};
